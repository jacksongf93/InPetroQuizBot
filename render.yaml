services:
  - type: web
    name: inpetro-quizbot
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --workers 1 --threads 8 --timeout 120 app:app
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
